import React, { Component } from 'react'
import { View, Text } from 'react-native'

export default class ForgotPasswordScreen extends Component {
	render() {
		return(
      <View>
        <Text>Forgot Password</Text>
        <Text>skip for now, not priority</Text>
      </View>
		)
	}
}